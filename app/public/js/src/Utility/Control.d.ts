export default class Control {
    static Running: boolean;
    private static AnimationFrame;
    static Init(renderUpdate: number, physicsUpdate: number): void;
    static Start(): void;
    static Stop(): void;
    private static Run;
}
