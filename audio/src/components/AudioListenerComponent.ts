import { UniqueComponent } from "@fwge/core"

export class AudioListenerComponent extends UniqueComponent
{
    constructor()
    {
        super()
    }
}

Object.setPrototypeOf(AudioListenerComponent.prototype, null)
