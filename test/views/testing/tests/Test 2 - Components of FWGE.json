{
	"id": 2,
	"description": "Tests of the components of FWGE.",
	"init": [],
	"finish": [],
	"tests":
	[
		"return typeof FWGE === 'object';",

		"return typeof FWGE.Item === 'object';",
		"return typeof FWGE.Maths === 'object';",
		"return typeof FWGE.Physics === 'object';",
		"return typeof FWGE.Render === 'object';",
		"return typeof FWGE.Convert === 'object';",
		"return typeof FWGE.Other === 'object';",
		
		"return typeof FWGE.Init === 'function';",
		"return typeof FWGE.Start === 'function';",
		"return typeof FWGE.Stop === 'function';",

		"return FWGE.Init.length === 1;",
		"return FWGE.Start.length === 0;",
		"return FWGE.Stop.length === 0;"
	]
}